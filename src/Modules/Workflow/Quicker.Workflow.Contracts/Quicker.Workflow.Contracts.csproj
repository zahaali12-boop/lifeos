<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <Description>Workflow module contracts (ADR-0020): the engine every document submits to for approval, the subject providers modules register (fields for rules, the callback on decision), blocks and the overrides that clear them.</Description>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="../../../Kernel/Quicker.Kernel/Quicker.Kernel.csproj" />
  </ItemGroup>
</Project>
