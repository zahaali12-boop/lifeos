# Assumptions and open questions

Every decision made where the brief is silent, plus the few questions that genuinely need the founder's answer.
Assumptions are numbered `A-nnn` and referenced from ADRs, the domain model and the roadmap.
When the founder answers a question, the answer is recorded here and the assumption is marked **confirmed** or **changed**.

Status legend: `proposed` (my call, proceeding), `confirmed` (founder agreed), `changed` (founder overruled; see note).

**2026-09-22: the founder approved the blueprint without answering Q1–Q8, so every default below applies and is marked `confirmed`.** Questions stay listed so answers can still change them later (mark `changed` with a note).

---

## 1. Questions for the founder (answer these; everything else proceeds on my defaults)

| # | Question | Why it matters | My default if unanswered |
|---|----------|----------------|--------------------------|
| Q1 | Is **Quicker** the final product name, and is there a brand (logo, colours) to follow? | Name appears in package names, tenant URLs, print templates and docs. Renaming later is cheap but noisy. | Product name "Quicker"; code namespace `Quicker`; neutral brand palette until told otherwise. |
| Q2 | This repository (`lifeos`) currently contains an unrelated single-file "Life OS" mobile app (`index.html`, `capacitor.config.json`, `package.json`, `build-apk.yml`). May I move those four files into `legacy/lifeos/` in M1 so the ERP owns the repository root? Or should the ERP live in a new repository? | The ERP needs the root for its monorepo layout, CI workflows and one-command setup. | Move them to `legacy/lifeos/` untouched, in the first M1 commit, and note it in PROGRESS.md. |
| Q3 | **Hosting region for the SaaS** and data-residency needs. Candidates: AWS Bahrain (me-south-1), AWS UAE (me-central-1), Azure UAE North/Qatar, or a European region. Saudi customers in regulated sectors may require in-Kingdom hosting. | Affects latency for Iraqi users, compliance (KSA PDPL, UAE PDPL) and cost. Reversible, but migrations are work. | Design region-agnostic; first environment on a Middle-East region; per-tenant region field from day one. |
| Q4 | **Launch vertical.** I assume the first paying customers are trading/distribution companies (import, wholesale, multi-branch retail-to-business) in Iraq. Is there a known first pilot customer whose needs should shape M4–M6? | Industry templates, demo data and the order of finance features (for example post-dated cheques before fixed assets). | Trading/distribution first; services and light assembly second. |
| Q5 | **Commercial model for SaaS**: per-user per-month subscription with plan tiers (Starter, Business, Enterprise) plus an on-premise licence. Any preference? | Determines the subscription and feature-flag model in M1/M8 and metering. | Per-user tiers with feature flags; annual on-premise licence key. |
| Q6 | **Identity providers** customers must sign in with: Microsoft Entra ID, Google Workspace, generic OIDC/SAML? | SSO adapters and testing in M1. | Built-in accounts with MFA, plus Microsoft Entra ID and Google via OIDC; SAML deferred to M8. |
| Q7 | **Official versus market exchange rates** (Iraq): many Iraqi businesses book at the Central Bank rate for statutory purposes and use the market rate operationally. Should the demo company keep IQD or USD as its functional currency? | Shapes the demo data and the default rate types. The engine supports both regardless. | Demo group: an IQD-functional trading company, a USD-functional import company and an AED-functional entity; rate types "official" and "market" seeded. |
| Q8 | **Arabic numerals**: Western digits (0-9) or Eastern Arabic digits (٠-٩) by default in the Arabic UI? | Formatting defaults; both are supported per user. | Western digits by default in Arabic UI, per-user toggle. |
| Q9 | **Iraq Unified Accounting System codes.** The `IRAQ_UAS` statutory chart ships with the nine top-level groups; which edition of the detailed code list (and which accounts of the group level) should statutory reports use, and is there an accountant who can validate the mapping? | The `IRAQ_UAS` template maps every postable account to a group today; detailed codes replace the group codes once confirmed. | Group-level mapping, marked provisional in the seed. |

---

## 2. Product and market assumptions

| ID | Assumption | Status |
|----|------------|--------|
| A-001 | **Launch definition.** "First paying customers" are trading and distribution companies (1–50 users, 1–5 legal entities, 1–20 branches/warehouses) in Iraq and the GCC. At launch they must run: chart of accounts and GL with IFRS statements; customers, suppliers, items; quotes to cash and requisitions to payment with partial deliveries and invoicing; multi-warehouse stock with FIFO/average costing, lots and serials; bilingual (EN/AR) invoices and statements; bank reconciliation and post-dated cheques; multi-currency with revaluation; roles, approvals and audit; report builder with drill-down; import of opening balances. That is milestones M1–M7 plus the import toolkit, custom fields and workflow items of M8 (see ROADMAP.md "Launch gate"). | confirmed |
| A-002 | **Deployment: both.** Cloud SaaS multi-tenant is the primary product; on-premise is the same container image run single-tenant. One codebase, one schema, no feature divergence. | confirmed |
| A-003 | **IFRS by default.** Default chart of accounts template, revenue recognition point, FX treatment (IAS 21), inventory (IAS 2: lower of cost and NRV, FIFO or weighted average; LIFO not offered), fixed assets (IAS 16) and leases (out of v1) follow IFRS for SMEs where simpler and full IFRS otherwise. Local statutory charts (Iraq Unified Accounting System, Saudi/GCC layouts) are mappings over the IFRS chart, with parallel adjustment books when required. | confirmed |
| A-004 | **Tax regimes seeded** for Iraq (no general VAT; sales tax on specified goods and services; contractor withholding), Saudi Arabia (15% VAT, ZATCA e-invoicing phase 2), UAE (5% VAT), Bahrain (10% VAT), Oman (5% VAT), Qatar and Kuwait (no VAT), Jordan (16% GST), Egypt (14% VAT with e-invoicing), Turkey (20% VAT) and a generic "EU VAT" template. Rates and rules are configuration, not code, and each template is validated by a local accountant before a customer goes live in that country. | confirmed |
| A-005 | **Working week** defaults to Sunday–Thursday for Iraq and GCC companies, Monday–Friday elsewhere; configurable per company and used for due dates, escalations and schedules. Public-holiday calendars are per company. | confirmed |
| A-006 | **Calendars.** Gregorian is the system calendar for all posting. Hijri (Umm al-Qura) is available for display and printing where a company enables it. Fiscal years may start in any month; a 13th adjustment period is supported. | confirmed |
| A-007 | **Languages.** UI in English and Arabic at launch, with full RTL. Master data fields that print on documents (item names, account names, partner names, UoM, payment terms, addresses) are bilingual, with automatic fallback to the other language. The i18n framework supports adding Kurdish (Sorani, RTL), Turkish and French later without schema change. | confirmed |
| A-008 | **Currencies.** Full ISO 4217 list seeded (over 160 codes) with minor units; company-level overrides for practical precision and cash rounding (for example IQD rounded to 250 or 1,000 dinars on cash documents, KWD/BHD/OMR at 3 decimals). | confirmed |
| A-009 | **Point of sale, manufacturing/MRP, HR/payroll and e-commerce connectors** are out of v1 as stated, but the model keeps hooks: the stock ledger accepts a `production` entry family, partners have an `employee` role, and sales orders carry a `channel` attribute. | confirmed |
| A-010 | **Not built in v1** and not pretended: payroll, project accounting beyond a project dimension, lease accounting (IFRS 16), hedge accounting, tax filing submissions (only reports and export files), electronic payment file generation (SWIFT MT101/pain.001) is M8 stretch, EDI. Each is listed in ROADMAP.md "Deferred". | confirmed |

## 3. Architecture and stack assumptions (see ADRs for reasoning)

| ID | Assumption | Status |
|----|------------|--------|
| A-020 | Backend in **C# on .NET 10 LTS**; PostgreSQL 17; front end in **TypeScript + React 19**. Reasoning in ADR-0002 and ADR-0013. | confirmed |
| A-021 | **Monorepo** with the .NET solution, the web app and infrastructure in one repository, one CI pipeline. | confirmed |
| A-022 | **Multi-tenancy:** shared database and shared schema with a `tenant_id` column on every tenant-scoped table and PostgreSQL row-level security, plus an optional dedicated-database tier for large or regulated tenants using the identical schema. On-premise is a dedicated database with one tenant. ADR-0004. | confirmed |
| A-023 | Infrastructure kept deliberately small: PostgreSQL, S3-compatible object storage, the API/worker containers and headless Chromium for PDF. **No Redis, no message broker** in v1; queues and the outbox live in PostgreSQL. A columnar store (ClickHouse) is introduced only if the M7 benchmark on PostgreSQL read models misses the 10M-line target. ADR-0021. | confirmed |
| A-024 | **Identifiers** are UUIDv7 everywhere; human-readable document numbers come from numbering series. Timestamps are `timestamptz` in UTC; posting dates are calendar dates in the company's time zone. ADR-0011. | confirmed |
| A-025 | **Precision.** Monetary amounts stored as `numeric(20,6)` and rounded to the currency's minor unit only when posted or printed; unit prices and unit costs `numeric(24,10)`; quantities `numeric(24,9)` in the item's base unit; exchange rates `numeric(24,12)`. Rounding: half away from zero by default, configurable per company (half even available). ADR-0005. | confirmed |
| A-026 | **Revenue recognition point** defaults to the invoice (the tax point in every target regime), with COGS recognised at shipment. A period-close checklist step accrues shipped-not-invoiced revenue (auto-reversing). Companies may switch the point to shipment. ADR-0026, POSTING_RULES. | confirmed |
| A-027 | **FX revaluation** of open AR/AP items posts unrealised gain/loss with automatic reversal on the first day of the next period (as the brief requires). Foreign-currency bank and cash balances are translated permanently (no reversal) because their carrying amount genuinely changes; this is configurable per company. ADR-0017. | confirmed |
| A-028 | **Reversals** post a mirrored journal with debits and credits swapped (never negative amounts), flagged `is_reversal` and linked to the original, so turnover reports can net or show both. Posting into a closed period is refused; corrections post in the first open period with a reference to the original. ADR-0026. | confirmed |
| A-029 | **Costing granularity.** Cost is tracked per company and item; a company may choose to value per warehouse instead. FIFO layers are per (company, item, warehouse, lot/serial). Moving average uses a daily average-cost period so backdated entries recompute deterministically. Standard cost variances post at receipt. Cost adjustments run synchronously after each posting; if more than 2,000 downstream entries must be re-applied, the run continues as an immediate background job with the item marked "valuation pending" (target completion: seconds). ADR-0008. | confirmed |
| A-030 | **Negative stock** is blocked by default. Companies may allow it per warehouse or item category, in which case issues are costed at the expected (last/standard) cost and adjusted when the receipt arrives. ADR-0008. | confirmed |
| A-031 | **Numbering.** Gapless series are allocated inside the posting transaction under a row lock; drafts carry a temporary draft number. Non-gapless series may allocate at creation. Per company, branch, fiscal year and document type. ADR-0016. | confirmed |
| A-032 | **API** is REST/JSON with OpenAPI 3.1, URL versioning (`/api/v1`), cursor pagination, `Idempotency-Key` on all mutating calls, ETags for optimistic concurrency, HMAC-signed webhooks. GraphQL is not offered; the semantic-layer query endpoint covers ad-hoc analytics. ADR-0012. | confirmed |
| A-033 | **Front-end grid and pivot** are built in-house on TanStack Table/Virtual rather than a commercial grid, to keep RTL, accessibility, licensing and drill-down fully under our control. ADR-0013. | confirmed |
| A-034 | **Authentication** is built-in (email + password with Argon2id, TOTP and WebAuthn passkeys for MFA) plus OIDC federation. Sessions use short-lived access tokens and rotating refresh tokens. ADR-0014. | confirmed |
| A-035 | **Audit log** is an append-only table with a per-tenant hash chain; the chain head is anchored daily to object storage with object-lock. ADR-0015. | confirmed |
| A-036 | **Custom fields** are stored in a JSONB column on every extensible entity, described by metadata that drives validation, UI, API and reporting. ADR-0019. | confirmed |
| A-037 | **PDF and print** are HTML/CSS templates (Scriban/Liquid syntax) rendered by headless Chromium; this is the only approach that handles Arabic shaping, bidi and fonts reliably. ADR-0022. | confirmed |
| A-038 | **Global search** uses PostgreSQL full-text and trigram indexes per tenant; an external search engine is not introduced unless measured. ADR-0023. | confirmed |
| A-039 | **Email** goes through an SMTP/API provider abstraction (any provider); SMS/WhatsApp notifications are a later adapter. | confirmed |
| A-040 | **AI provider** is abstracted; the reference implementation uses the Claude API. AI never executes raw SQL; it produces semantic-layer queries executed under the user's permissions, and it can only create drafts. ADR-0028. | confirmed |
| A-041 | **Backups**: point-in-time recovery with 35-day retention for SaaS; documented restore drill in M10. On-premise ships the same scripts. | confirmed |
| A-042 | **Licensing** of the codebase is proprietary (all rights reserved) until the founder decides otherwise; only permissively licensed dependencies (MIT/Apache/BSD/PostgreSQL) are used, checked in CI. | confirmed |
| A-044 | **Segregation of duties and super users.** The all-access grant `*` (owner, admin) is excluded from SoD conflict evaluation; members holding it are listed as super users in the SoD report instead. Every other grant, including module wildcards, is evaluated. | confirmed |
| A-045 | **Security bookkeeping survives refusals.** A request that ends in an error normally rolls back its unit of work; failed-login counters, lockouts, refresh-token reuse revocation and the audit events of refusals are committed anyway through an explicit `CommitOnFailure` flag set by the identity services. | confirmed |
| A-046 | **HTTP status comes from the error kind.** Domain errors carry a kind (validation 422, not found 404, conflict 409, forbidden 403, unauthorized 401, locked 423, rate limited 429); codes are stable strings and never parsed for status. Cross-tenant access always answers 404. | confirmed |
| A-047 | **API keys** are `qk_<tenant id>.<secret>`; only the SHA-256 of the secret is stored; a key acts as its creating member with optionally narrowed scopes and never as an owner. | confirmed |
| A-048 | **Emails are stored lower-cased** and compared exactly; sign-in is case-insensitive by construction. | confirmed |
| A-049 | **Default security policy**: 12-character minimum passwords, lockout after 10 failures for 15 minutes, 10-minute access tokens, 14-day sessions, 5-minute step-up window, MFA optional (tenant admins can require it). | confirmed |
| A-050 | **Two audit chains.** Every tenant has its own hash chain (`app.aud_events`); events recorded by a request that never enters a tenant (failed sign-in before tenant selection, password reset) go to one platform chain in `control`, readable by platform operators only. A request that starts anonymous and then enters a tenant (sign-up, invitation acceptance, SSO) carries its earlier events into that tenant, attributed to the person it ended up as. ADR-0015. | confirmed |
| A-051 | **Audit events are written at commit.** The sink buffers a unit of work's events and inserts them immediately before the transaction commits, so the per-tenant chain lock is held for microseconds instead of for the whole request; a rolled-back request leaves no events (refusals that must be kept use `CommitOnFailure`, A-045). | confirmed |
| A-052 | **Explicit beats captured.** EF change capture records every insert, update and delete of an audited entity; when a service also records an explicit event for the same record in the same unit of work (posted, revoked, invited), the explicit event is kept and inherits the captured before/after/diff, and the captured row event is dropped. Updates store only the changed fields in `before`/`after` plus a field-level `diff`; creates and deletes store the full snapshot. | confirmed |
| A-053 | **Hashing happens in the database, verification outside it.** The canonical text of an event is rendered by PostgreSQL (`app.aud_canonical`) and the chaining trigger computes `hash = SHA-256(prev_hash ‖ canonical)` under the tenant's head lock; the application role cannot set hashes or touch chain heads. The verifier recomputes every link in application code from the same rendering and compares the head and the newest anchor. A party with schema-owner rights who also rewrites the hashing function is outside the chain's threat model and is caught by anchors. | confirmed |
| A-054 | **Anchor store.** Chain heads are anchored to an append-only, hash-linked JSON-lines file (`Quicker:Audit:Anchoring`), the on-premise default; the object-lock store arrives with the storage client in M1.8 and the daily schedule with the scheduler in M1.8. Until then anchoring and verification run on demand (API) and through the platform-wide job entry points. | confirmed |
| A-055 | **Secrets never enter the log.** Entity properties marked redacted, and any payload property whose name denotes a secret (password, secret, token, hash, key), are replaced by `[redacted]`; a change to a redacted field is still visible in the diff. Login counters and last-login stamps are ignored by capture because sign-in has its own events. | confirmed |
| A-043 | **Performance budgets**: p95 under 300 ms for everyday API calls with 50 concurrent users per tenant on a 4-vCPU database; interactive pivots over 10M fact rows under 5 s; document posting under 500 ms p95 including cost adjustment for typical chains. Verified in M10 with seeded data. | confirmed |

## 4. Process assumptions

| ID | Assumption | Status |
|----|------------|--------|
| A-060 | The founder reviews at the end of each milestone; within a milestone I proceed on documented defaults and stop only for expensive-to-reverse decisions. | confirmed |
| A-061 | Every session starts by reading `CLAUDE.md`, `docs/PROGRESS.md` and the ADR index, and ends by updating `docs/PROGRESS.md` and committing. | confirmed |
| A-062 | Demo data is generated by a deterministic seeder (fixed random seed) so demos and performance tests are reproducible. | confirmed |
| A-063 | Documentation language is English; UI strings are authored in English and translated to Arabic in the same pull request that adds them (no untranslated strings reach `main`). | confirmed |
| A-064 | **ISO currencies are platform reference data** (`control.currencies`, seeded by the migrator, read-only for the application). Tenants enable currencies per company (`org_company_currencies`) with their own display precision and cash rounding increment; the functional currency is always enabled and is immutable once the company exists. | proposed |
| A-065 | **Fiscal year codes**: `FY2026` for a calendar year, `FY2026/27` for a year spanning two calendar years. Creating a company opens the fiscal year that contains today (in the company's time zone) when its calendar has none; later years are opened explicitly. The 13th (adjustment) period shares the last day of the year and is never selected by posting date. | proposed |
| A-066 | **Period states** are stored only where set; a period without a row is `open` in an open year, `never_opened` in a future year and `hard_closed` in a closed year. Ordinary changes (open ↔ soft_closed → hard_closed) need `accounting.period.manage`; leaving `hard_closed` needs `accounting.period.reopen`, a reason and recent authentication, and is audited as an override. | proposed |
| A-067 | **Exchange-rate resolution order**: identity, direct rate, inverse of the direct rate, cross rate through the company's functional currency (both legs direct or inverse). Correcting or deleting a stored rate requires a reason. Provider imports never overwrite a rate entered manually on the same date. | proposed |
| A-068 | **Tenant defaults** created at sign-up: fiscal calendar `standard` (January, 12 periods), business calendars `sun_thu` (default) and `mon_fri`, system dimensions `BRANCH`, `COST_CENTER` (hierarchical), `DEPARTMENT` (hierarchical), `PROJECT`, rate types `spot`, `closing`, `average`, `budget` (system) plus `official` and `market`, and the units PCS, DZ, CTN, PLT, KG, G, TON, L, ML, M, CM, M2, HR, DAY with their conversions. A branch's dimension value is coded `<COMPANY>-<BRANCH>`. | proposed |
| A-069 | **Global unit conversions** are defined once per pair within a family and resolved directly, inversely, or through one intermediate unit; conversions across families (KG ↔ PCS) are item-specific and belong to Items (M2). | proposed |
| A-070 | **Numbering counters are per series and reset period** (`''`, the fiscal year code such as `FY2026/27`, or `2026-09`), advanced by `app.num_allocate` under a row lock inside the caller's transaction; every issued number is recorded append-only with its document, so duplicates are impossible at the database and the gapless audit is a query. A series starts every period at its `start_number`. | proposed |
| A-071 | **Series selection**: candidates are active series of the document type and company valid on the date whose branch (if set) matches; a branch-bound series scores 2, a fiscal-year-bound one 1; the highest score wins, then `is_default`, then code. A document may override to any applicable series. Templates use `{company} {branch} {yy} {yyyy} {mm} {fy}` and exactly one `{seq}`/`{seq:N}`; `{branch}` requires a branch-bound series. | proposed |
| A-073 | **Outbox ordering is strict per aggregate**: a message is claimed only when every earlier message of its aggregate is published, dead letters included, so a poison message halts its aggregate until an operator retries it; other aggregates continue. Handlers are idempotent through `ops.inbox` (row inserted in the handler's own transaction). Events with no registered handler are marked published immediately. | proposed |
| A-074 | **Jobs**: the final state is written in the job's own transaction (effect and completion commit together); a claim counts as an attempt, a graceful shutdown hands the job back without counting it, a lost heartbeat (2 minutes) requeues it, `JobFailedException` ends it as `failed` with no retry, other exceptions retry with backoff (1 s → 1 h) until `max_attempts` (default 5) and then `dead`. Job idempotency keys are unique per tenant and job type. | proposed |
| A-075 | **Schedules** use five-field cron in a named time zone; a new or re-enabled schedule is armed for its next slot without firing for the past; slots missed while no worker ran collapse into one job; one scheduler instance ticks at a time (advisory lock). Platform schedules (no tenant) are seeded: audit anchoring 02:00 UTC, verification 03:00 UTC, outbox archive 01:30 UTC, idempotency sweep hourly. | proposed |
| A-076 | **Idempotency scope and hash**: a key is scoped to (tenant, principal = actor type + membership); the request hash covers method, path, query string and body; the stored response includes 4xx outcomes (a retried refusal gets the same refusal) but never 5xx; keys expire after 24 hours and are swept hourly. Concurrent requests with one key serialise on a PostgreSQL advisory lock so exactly one executes. | proposed |
| A-077 | **Webhook signing**: `X-Quicker-Signature: v1=<hex HMAC-SHA256(secret, "<unix timestamp>.<body>")>` with `X-Quicker-Timestamp`; secrets are `whsec_` + 32 random bytes, shown once, stored AES-GCM encrypted, rotatable. Deliveries retry with the job backoff (1 s → 1 h) for 30 attempts (about a day) and are then `dead`; an operator can replay any delivery. Event-type patterns are exact, prefix (`sales.*`) or `*`. | proposed |
| A-078 | **Notification channels**: every member gets in-app and email by default; a preference row per kind (or `*`) switches either off; emails are rendered in the member's locale with the tenant default as fallback and carry the link on a last line; email sending is a job with the platform backoff for 10 attempts, and the identity module's transactional emails (invitations, resets) bypass the log. | proposed |
| A-079 | **Object storage layout and immutability**: keys are `tenants/<tenant>/attachments/<id>` for files and `immutable/audit-anchors/...` for anchors; the `immutable/` prefix maps to a separate object-lock bucket (compliance mode, 10-year retention by default) so anchors cannot be shortened or deleted by anyone including the platform; uploads are capped at 25 MB by default (`Quicker:Storage:MaxUploadBytes`), hashed with SHA-256 and served with their original file name. | proposed |
| A-080 | **Custom-field values live in the host row** (a jsonb column per host table, `company` first) and are validated by the Collaboration definitions before every save; an `indexed` definition creates a shared expression index `(tenant_id, (column->>'key'))` on the host table through the owner connection, so a tenant's choice adds a physical index that serves every tenant's rows for that key. Key, entity type and type are immutable once defined. | proposed |
| A-081 | **Comments, activities, links and views are tenant-wide by permission**, not per record, until document modules bring record-level permissions; comments are soft-deleted (body cleared, thread kept) and mentions notify through the standard channels. | proposed |
| A-082 | **API contract policy**: `contracts/openapi-v1.json` is the contract; additive changes (new paths, optional properties, new enum values) ship freely, anything the checker flags as breaking (removals, new required inputs) needs `/api/v2` or a deprecation window, and the generated TypeScript client is the only way the web app calls the API. Until the first tagged release the contract may break with a note in PROGRESS; the CI gate compares against `main`. Lists that can grow use `{items, nextCursor}` envelopes with opaque cursors; filters are `field op value [and …]` with per-endpoint field whitelists; `?fields=` shapes responses, `?expand=` embeds relations; GETs carry weak ETags and resources with versions honour `If-Match`. | proposed |
| A-083 | **Rate limit defaults**: 600 requests/minute per principal, 120 per anonymous address, 20 per address on the auth endpoints; sliding windows per node; exceeding answers `429 rate.limited` with `Retry-After`. Numbers are placeholders until real usage is measured. | proposed |
| A-084 | **Web app conventions**: the browser keeps only the session (tokens, tenant, user) and per-device conveniences (language, digits, theme, recent records) in local storage; everything else is server state through TanStack Query and the generated client; Arabic is a complete resource file checked in CI; the document `dir`/`lang` follow the UI language, not the user's locale in the profile (they converge when the profile screen lands); Eastern Arabic digits are a display preference only, the API always exchanges ASCII digits. | proposed |
| A-085 | **CORS**: the API allows the origins in `Quicker:Api:AllowedOrigins` (comma separated; default the auth public origin) with any header and method, and exposes `ETag`, `Retry-After`, `Idempotent-Replayed` and `Location`; credentials are bearer tokens, never cookies, so no credentialed CORS. | proposed |
| A-086 | **Telemetry conventions**: spans carry `quicker.tenant_id`, `quicker.user_id`, `quicker.membership_id`, `quicker.actor` and `quicker.request_id` as ids only (never names or emails); job spans are `job <type>` and event spans `event <type>` of kind consumer; the meter is `Quicker.Platform`; health probes are excluded from traces; readiness thresholds are 5 minutes of outbox lag and 1000 overdue jobs until real load says otherwise. | proposed |
| A-087 | **Demo tenant v1** (ADR-0029, Q7): slug `demo`, group "Quicker Demo Group"; companies `IQT` Al-Rafidain Trading (IQ, IQD functional, USD reporting, branches Baghdad/Basra/Erbil), `USI` Tigris Import (IQ, USD functional, IQD reporting, Baghdad), `AEG` Gulf Gate General Trading (AE, AED functional, USD reporting, Dubai); users `<role>@quicker.example` with the shared password `DemoPass2026!`; rate types `official` (CBI 1310 USD/IQD, fixed) and `market` (bounded walk around 1500 from a fixed seed) beside the system `spot`/`closing`/`average`/`budget`; USD/AED pegged at 3.6725. The series always ends today, so the walk is deterministic per calendar date rather than per run date. Tenant, user and membership ids are fixed (version-7 layout from a fixed epoch and a name hash); other ids come from the services. `make up` creates the tenant when missing and never overwrites it; `make demo` rebuilds it. | proposed |
| A-088 | **Chart of accounts model**: a chart is shared by the tenant's companies or dedicated to one (`gl_charts.company_id`); a company's chart is `org_companies.chart_id`, owned by Organization and set through `ICompanyDirectory.AssignChartAsync` after Accounting validates the chart. Accounts carry a `default_role` (the posting role the account is the chart's default for) so a posting profile can be derived from any template or import; `subtype`, `cash_flow_category` and `currency_restriction` are free data for reports and validation. Three templates ship: `IFRS_SME` (109 accounts, four-digit codes: thousands = type, hundreds = group), `GCC` (adds VAT naming, Zakat payable and expense) and `IRAQ_UAS` (IFRS layout mapped to the Unified Accounting System groups). Control accounts of every template refuse manual journals by default (POSTING_RULES §8). Categories are per tenant and seeded with the first template. | proposed |
| A-089 | **Posting engine details** beyond ADR-0006/0007/0017: (1) a journal entry has no mutable status; "reversed" is the `gl_entry_links` row, and a unique index allows one reversal per entry; (2) `gl_journal_lines` is partitioned by posting year, so its primary key is `(tenant_id, posting_date, id)` and later subledger rows reference a line by id and posting date; (3) reporting-currency amounts are the transaction amounts when the reporting currency is the transaction currency, the functional amounts when it is the functional currency, otherwise functional × the fc→rc rate, each line rounded with the company's rounding policy and the entry closed with a `RoundingDifferences` line carrying no transaction amount; (4) journal entries are numbered by a per-company series `JE-{yyyy}-{seq:6}` (gapless, yearly reset) that the engine creates on first use through `INumberAllocator.EnsureDefaultSeriesAsync`; administrators may replace it; (5) the company ↔ chart and company ↔ posting profile foreign keys are deferrable so a tenant purge can delete both sides; (6) a system actor (jobs, seeders) may post into soft-closed periods, a user needs `accounting.period.post_in_soft_closed`; hard-closed and never-opened refuse everyone with the reopen permission named; (7) a rate override on a request needs a reason and is stored on the entry. | proposed |
| A-090 | **Manual journals and routines**: (1) approval is a company setting `accounting.journals.approval` (`none` by default, `required` makes submit wait for an approver who is not the submitter, `accounting.journal.approve`); (2) journals are numbered `MJ-{yyyy}-{seq:5}` per company at posting time (gapless, yearly reset), drafts show `DRAFT-…`; (3) a manual line may touch a control account only when it names the subledger item it adjusts, so subledgers stay reconciled (the engine's manual-posting block applies to lines without one); (4) opening journals balance themselves on the `OpeningBalanceEquity` role and are flagged `is_opening_entry`; (5) accruals carry `auto_reverse_on` and the daily routine reverses them on that date (into the first open period when it is closed) as `auto_reversal_of`; (6) recurring templates use a cron expression evaluated in the company's time zone, amounts fixed, as percentages of a base amount (the last credit line absorbs the rounding) or variable (always reviewed); (7) deferral schedules amortise to the minor unit with the remainder on the last line, one line per fiscal period (calendar month ends when the company has none yet), straight-line or by days; (8) the platform schedule `accounting.daily` runs the routines at 00:30 UTC for every active tenant and reports each waiting item with its reason instead of skipping it; (9) a company's first posting profile version is effective from the beginning of time so opening balances and back-dated go-live entries post; later versions start today unless dated. | proposed |
| A-091 | **Period control details** (ADR-0026): (1) allow-posting windows are stored per company for everyone (`role_id` null) or per role, each with at least one bound; a person's effective window is the widest of their roles' windows when any role has one, else the company's; system actors (jobs, seeders) are exempt, owners are not; the check runs after the period state check, so a closed period is reported as such before any window; (2) a correction is reversal plus replacement in one transaction: the reversal takes the original date when its period is open for the actor, else the first day of the first open period, and the replacement takes the later of its own date and the reversal date; the replacement is linked `corrects` → original next to the reversal's `reverses`; an entry is corrected once, a reversed entry is not corrected (post a new document); (3) a manual journal's correction draft copies the lines, keeps the kind, currency, rate and reference, drops an automatic reversal date that is not after the new posting date, and is numbered like any journal at posting; (4) reopening a hard-closed period keeps the 1.6 rule: `accounting.period.reopen`, a reason, recent authentication, audited as `override` with the actor. | proposed |
| A-092 | **GL inquiry** (roadmap 2.5): the trial balance and ledger read the journal lines (functional or reporting amounts), never the derived balances, so an inquiry is exact at any date and the derived table is only a cache verified by the harness; "balanced" means a zero opening total and equal movements over the window; a comparative uses the same window length ending on the comparative date; dimension filters and grouping read the dimension set values (`org_dimension_sets`, `org_dimension_values`) directly as reporting reads, which ADR-0021 allows for the reporting layer; closing entries are excluded by default; exports are CSV or XLSX generated in process (no spreadsheet library), with numbers as number cells and dates as date cells, audited as `exported`; the ledger export is capped at 10,000 lines until the reporting platform streams. | proposed |
| A-093 | **Day-one posting and profile versions**: creating a chart from a template for a company also creates and activates that company's first posting profile from the chart's default accounts when it has none (the two calls remain available separately); the engine resolves an entry against the company's current profile when it is effective on the posting date, otherwise against the latest non-draft version effective on that date, so a newer version dated today never leaves earlier dates without rules. In the ledger, a line without its own description shows the description of its entry. The accounting web screens keep the working company per browser and default to the first company. | proposed |
| A-094 | **Invariant harness v1** (ADR-0029): the product module is `Quicker.Integrity` (the `*.Invariants` project suffix is reserved for test projects by the build), exposing `IInvariantHarness`; a run is read-only, scoped to the tenant of the unit of work by row-level security, optionally to one company, and reports every check with a count of what it examined and at most 20 problems; "balanced" tolerates nothing (exact decimal equality); the derived-balance check compares the stored rows with a rebuild in both directions so a missing or extra row is a problem too; isolation is checked from the catalogue (`ops.tenant_tables`) because a tenant context cannot see other tenants' rows by construction; gapless numbering is checked per series and period from the allocations and the counter; the daily job runs at 03:30 UTC after the audit verification and fails naming each failing tenant; the permission is `platform.integrity.run` (sensitive); scenario tests assert the harness at their end through the API. | proposed |
| A-095 | **Demo tenant v2** (roadmap 2.7, ADR-0029): the same `demo` tenant of A-087 carries a year of books per company, posted through the accounting services as the system actor inside the seeder's transaction, never written directly. Charts: `IQT` from `IRAQ_UAS`, `USI` from `IFRS_SME`, `AEG` from `GCC`, each with its first posting profile from the chart's default accounts. Dimensions: cost centres `CC-ADM`, `CC-SLS`, `CC-OPS`, `CC-LOG` (salary shares 30/30/25/15), departments `FIN`, `HR`, `IT`, projects `PRJ-ERP`, `PRJ-BSR`, and the branches. Parties: six customers and five suppliers with fixed version-7 ids, used as subledger items on the AR and AP control accounts until the partner masters of M3–M5 own them. Books: the window is the twelve months ending in the current month; figures are USD amounts scaled to the functional currency (IQD ×1310 rounded to the dinar, AED ×3.67 rounded to the fils) so the three sets differ; an opening journal, an insurance prepayment amortised over 12 periods, a deferred maintenance contract (`AEG`), a monthly recurring rent template, and per month a sale, a purchase, utilities, a collection, a payment, salaries by cost centre, a month-end accrual reversed automatically the next day, bank charges, quarterly rent and a consultants journal on the ERP project; the daily routines run once at the end so nothing is left waiting; months before the previous one are hard-closed, the previous month soft-closed, the current month open; `IQT`'s eighth-month purchase is corrected through the journal correction path; the invariant harness must pass before the seed commits. Counts are deterministic per calendar month, so the reseed test asserts identical journal and entry counts across runs. | proposed |
| A-096 | **Item master** (roadmap 3.1, DOMAIN_MODEL §8): (1) every item has a base unit and one row per unit in `itm_item_uoms`, the base at 1/1, the others with an exact rational factor to the base stored as `numeric(24,12)`; a quantity converts as quantity × numerator ÷ denominator and is accepted only when it is a whole number of the target unit's precision (from `org_uoms.precision`), never rounded, so the carton/piece/dozen chain of hard scenario 9 cannot drift; (2) the base unit cannot change while other units exist and cannot be removed; sales and purchase units must be units of the item; (3) barcodes are unique across the tenant, belong to a unit and optionally a variant, and EAN-13/EAN-8/UPC-A check digits are verified; (4) variants are defined by attribute values (attribute code → value) with a unique SKU and a unique combination per item, and an attribute value in use cannot be removed; (5) categories carry a materialised path of codes and may override the costing method and the item posting group; items themselves have no costing override (the category's applies, then the company's method, ADR-0008); (6) references to modules not yet built are plain uuids: supplier partner ids, tax groups, warehouses and bins (the warehouse foreign key arrives with 3.2), image attachments; (7) a bill of material is a kit (issued at shipment) or an assembly (built), must match the item type, has one active version per item, allows only stock, kit or assembly components in one of their units, refuses cycles through nested active bills, and explodes multi-level with scrap as a percentage per line; (8) the item permissions live under `inventory.item.*` and `inventory.bom.*` because the role templates already grant `inventory.item.read` to sales, purchasing and warehouse roles, and a new `inventory_manager` template holds `inventory.*`. | proposed |
| A-097 | **Stock ledger and balances** (roadmap 3.2, ADR-0008, DOMAIN_MODEL §8): (1) `inv_stock_ledger_entries` is the quantity side of the ledger and is append-only; entries are in the item's base unit after the exact conversion of A-096 and keep the entered quantity and unit; the sign of an entry is fixed by its type (receipts, returns in, transfers in, positive adjustments, count variances up, assembly output, consignment in and openings are positive; shipments, returns out, transfers out, negative adjustments, scrap, assembly consumption, consignment out and drop-ship are negative); (2) a posting is one document movement in one transaction and is idempotent on `(source document type, source document id, idempotency key)`, so a retried request replays the same entries; (3) `inv_stock_balances` is a derived cache keyed by company, item, variant, warehouse, bin, lot and serial with nil uuids for "none"; a movement inserts the missing row, locks every row it touches with `SELECT … FOR UPDATE` in a fixed key order, and applies the policy under the lock; the invariant harness rebuilds on hand and reserved from the ledger and the open reservations; (4) the negative-stock policy is resolved item setting for the company → warehouse override → company policy; `block` refuses with `409 stock.insufficient` and a `why` naming on hand, reserved, requested and the policy source; `allow` posts; `approve` behaves as `block` until the approval workflow (M3.4) lands; (5) availability is on hand − reserved − quality hold; a reservation belongs to a source document, reduces availability for everyone else, expires on its date through the daily `inventory.reservations.expire` job (00:15 UTC), and is consumed, in one or several shipments, when the same document ships (each shipment takes its own reservation before checking availability and the reservation closes once its quantity is consumed); (6) a two-step transfer ships from the source into the company's in-transit warehouse and receives from there into the destination, in parts or in full, each step a pair of entries linked by `transfer_pair_id`; in-transit quantity per destination is derived from open transfers; a transfer may name its transit warehouse; otherwise the company's only in-transit warehouse is used and several are refused as ambiguous; transfers are numbered `TRF-{yyyy}-{seq:5}` per company; (7) inventory periods follow the accounting period machinery under module `inventory`: a system actor may post into soft-closed periods, a user needs `inventory.period.post_in_soft_closed`, hard-closed refuses everyone; (8) warehouse scopes on a role restrict every stock read and movement to the scoped warehouses, company scopes to the company; (9) warehouse kinds are `standard`, `in_transit`, `quarantine`, `consignment` and `virtual`; bins are enabled per warehouse (never on in-transit), and a bin holding stock cannot be deleted. | proposed |
| A-098 | **Costing engine** (roadmap 3.3, ADR-0008): (1) the stock ledger is append-only, so an inbound entry's remaining quantity is derived from its active applications and whether an entry was costed at an expected cost is a fact of its value entries, not a column of the movement; the movement carries its inputs instead (`entered_unit_cost` per entered unit in the functional currency, `cost_is_expected`, `applies_to_sle_id`); (2) the cost scope lock is the row of `inv_item_cost_scopes` (company, item, warehouse or nil), taken with `SELECT … FOR UPDATE` before any valuation or re-application, and the same row carries the `valuation_pending` flag; (3) value entries carry `account_role` (Inventory, InventoryInTransit for an in-transit warehouse, or the variance account for a variance row) and `offset_role` with `offset_ref` (the receipt for GRNI, the trigger document for LandedCostClearing), so the journal is derived from the rows themselves; inventory value = Σ rows on the inventory roles; (4) within one day, inbound entries are applied before outbound ones (each by sequence), so a day's receipts are available to that day's issues under every method and the result does not depend on the order the day's entries were posted; a FIFO issue applies to layers dated on or before its own date, and a shortfall (negative stock, or a shipment dated before its receipt) is valued at the expected cost (the scope's last cost, else the standard, else zero), flagged, and applied forward to the earliest later layer when one exists; (5) money is rounded per application and per value entry to the functional currency's minor unit with the company's rounding mode; the last unit taken from a layer (or from an average pool) takes the whole remaining value, so zero quantity is zero value; unit costs are stored unrounded; (6) the expected cost stays expected until an invoice settles the receipt; issues from an uninvoiced layer are actual cost at the expected unit cost (no expected/actual split through applications); (7) a standard cost version is data under FIFO and average (a fallback expected cost) and the valuation basis under standard cost, where a new version revalues the quantity on hand at its effective date against InventoryWriteDown and re-applies later movements; the 3.1 item-per-company `standard_cost` setting is the fallback when no version is effective; (8) the re-application threshold is a platform setting `Quicker:Inventory:RecostThreshold` (2,000 entries); beyond it the triggering call queues the run and defers the whole valuation of that scope to the job; (9) the GL date of an adjustment follows ADR-0008 §5 with the inventory period as the test (a system actor and a user with `inventory.period.post_in_soft_closed` may post into soft-closed periods); the accounting engine still applies the GL period, so a closed GL period fails the posting and the whole movement rolls back; (10) journals from the costing engine use source module `inventory`, the movement's source document as their document (the trigger document for settlements, `cost_adjustment_run` for background runs), one journal per GL date and scope walk, and are idempotent by a key made of the document, the date, the run and the walk. | proposed |
| A-099 | **Stock documents** (roadmap 3.4): (1) reason codes are tenant-wide, apply to one document family (adjustment, count, return, scrap, write_off, shortage) and may redirect the movement's offset to one of the inventory expense accounts only (InventoryAdjustment, CountVariance, Scrap, InventoryWriteDown, Cogs), which the costing engine honours through `offset_role_override` on the stock ledger entry; (2) adjustment approval is the company setting `inventory.adjustments.approval` (`none` by default, `required` makes submit wait for an approver other than the submitter with `inventory.adjustment.approve`), a single-step approval until the workflow engine (M6) takes documents over; (3) stock taken out (negative, scrap, shortage) is always valued at the applied cost and never carries an entered cost; stock brought in (positive, opening) carries the document's cost or, when none is given, the item's current cost in its scope; (4) an NRV write-down brings every open FIFO layer to the new unit value and, on reversal, never above that layer's own original cost; in a cost pool the reversal is capped at the current unit cost plus the write-downs still attached to the units on hand (each earlier write-down per unit of the quantity it applied to, issues having carried their share out), an approximation of IAS 2 that is exact when no issue happened between the write-down and the reversal; (5) a revaluation is a document per company with a line per item and scope, numbered `RVL-{yyyy}-{seq:5}`, posted straight from draft by `inventory.costing.manage` (no approval step); (6) a one-step transfer posts `transfer_out` and `transfer_in` into the destination in one posting and is received at once; a shortage declared on a two-step receipt is written off from the transit warehouse as a `negative_adjustment` with a shortage reason code, so in-transit value and the InventoryInTransit account agree at every date; (7) an assembly build consumes the direct components of the item's active assembly bill (base units, scrap allowance included; sub-assemblies are built by their own documents) or the lines given, in one stock posting with the output; the output is worth what the components cost, with the assembly variance account used only under standard cost; a later change to a component's cost re-costs the consumption and cascades to the assembly through the re-application queue; (8) documents are numbered at posting (`ADJ-`, `RVL-`, `ASM-` per company, yearly reset) and show `DRAFT-…` before; a posted document is corrected by another document, never edited or cancelled. | proposed |
| A-100 | **Lots and serials** (roadmap 3.5, DOMAIN_MODEL §9): (1) lots are per item, unique by number, created by the first receipt that names them (or ahead of it through the API); the expiry comes from the line, else from the item's shelf life counted from the posting date, and is required when the item requires expiry; the supplier lot and partner are kept from a purchase receipt; (2) an issue of a lot-tracked item names its lot; when it does not, the engine refuses and answers with the first-expiry-first-out plan (active lots with available stock in the warehouse, earliest expiry first, undated last), so picking stays the document's decision and never silent; (3) quarantined, recalled and expired lots are on quality hold on every balance row (available = on hand − reserved − hold), their open reservations are released, and their stock cannot be shipped, transferred or consumed; scrap, negative adjustments, returns to supplier and receipts (a customer return of a recalled lot) still move them; a lot expires by date (`expires_on` before the posting date) even before the daily job marks it; (4) a recall requires a reference and answers with its impact (stock on hand per warehouse, receipts, shipments and the customers with quantities), the same data as the lot's trace; (5) serials are per item, unique by number, whole units in the base unit, one ledger entry per unit so each serial has its own balance row, FIFO layer, cost and timeline; a serial's status is derived from its last movement (received/adjusted → in_stock, into transit → in_transit, shipped → sold, customer return → returned, purchase return → returned_to_supplier, scrap/negative/count → scrapped, consumed by an assembly → consumed, consigned out → consigned) and only repair is set by hand (returned or in stock → in repair → in stock), a unit in repair being on hold; (6) the counterparty of a movement (`partner_id` on the ledger entry) is what the calling document passes; the lot trace groups shipments by it and the serial keeps it as its current holder while sold or consigned; (7) lot and serial custom fields use hosts `lot` and `serial`; (8) the platform job `inventory.lots.expire` runs daily at 00:20 UTC across tenants. | proposed |
| A-101 | **Stock counts** (roadmap 3.6, DOMAIN_MODEL §9.2): (1) a count belongs to one warehouse and one posting date; its freeze snapshots the balance rows with stock (per item, variant, bin, lot and serial) under a row lock together with the global ledger sequence, so the snapshot and the sequence agree to the entry; (2) movements during the count are allowed unless the count blocks them; at review, approval and posting the engine reads the movements posted since the freeze for each key (sequence after the freeze, posting date on or before the count date, the count's own entries excluded) and the variance is counted − (expected + movements), so a count never reverses a movement that happened while it ran; movements dated after the count date are not part of it; (3) a blind count hides expected quantities and variances on the sheet until the review; (4) stock counted where nothing was expected (a bin, a lot, a serial) becomes a line with nothing expected and a positive variance; a serial is counted present (1) or missing (0), and a missing serial is scrapped by the posting; (5) a recount keeps the previous count on the line and holds the review until the line is counted again; (6) every variance needs a reason code that applies to counts before approval, and the reason's account override redirects the variance from CountVariance to that account; (7) the count posts as one stock posting of signed `count_variance` movements dated on the count date, valued by the costing engine (found stock at the current cost, missing stock at the applied cost), and is numbered `CNT-{yyyy}-{seq:5}` at freeze; (8) a count that blocks movements blocks every movement into or out of its warehouse, or only its bins for a bins scope, until it is posted or cancelled; (9) the cycle scope takes the items whose warehouse settings put them in the given classes (A, B, C), so the classes are data of the item master, not a schedule of their own yet (cycle-count scheduling lands with replenishment planning). | proposed |
| A-102 | **Replenishment** (roadmap 3.7): (1) the planning parameters are the item's warehouse settings of 3.1 (reorder point, min, max, safety stock, lead time) and its preferred supplier (lead time when the warehouse setting has none); an item is planned in a warehouse when it has a reorder point, a minimum or a maximum there; (2) projected stock = on hand − reserved − quality hold + transfers in transit to the warehouse + incoming supply reported by purchasing (`IIncomingSupply`, nothing until M4); (3) the trigger is the reorder point when set (projected ≤ reorder point), else the minimum (projected < min), else the maximum (projected < max); the suggested quantity is max − projected when a maximum exists, otherwise reorder point + safety stock − projected, and never leaves the projection below the minimum; the date needed is the run date plus the lead time; (4) one open suggestion per item and warehouse: a later run refreshes it (quantity, supplier, date, explanation) or closes it as superseded when the need is gone, and accepted or dismissed suggestions are left as decided (a new need after an acceptance creates a new suggestion, since the accepted one is not yet a purchase order the planner can see); (5) the planner runs daily at 01:00 UTC for every active tenant and company through the platform schedule, and on demand per company or warehouse; (6) suggestions explain themselves with every input and the formula, so the purchasing screen and the intelligence milestone read the same data. | proposed |
| A-103 | **Mobile scanning** (roadmap 3.8): (1) the scanner is a route group of the web app (`/m`) installed as a PWA, on the same session and permissions, not a separate app; (2) a capture sets the counted quantity of its bin/item/lot/serial line (the count API's "last entry wins"), so counting the same line twice on the device sets it twice rather than adding; (3) a code the device cannot resolve offline is captured as typed and resolved by the API on sync; an unknown code surfaces in the queue as a failed capture for the operator to fix or drop; (4) the queue replays strictly in capture order; a business rejection does not block the captures behind it, a network failure does; (5) the mobile transfer is a one-step transfer between warehouses shipped on sync with the company's current date; (6) codes and bins resolve on the device only after being seen online once (a bounded cache of 500 codes), and the offline app shell comes from a production build's service worker (nothing is cached in `pnpm dev`). | proposed |
| A-104 | **Demo seed v3** (roadmap 3.9): (1) items are tenant-wide and stock is per company: food, home, pharmacy and industrial families are stocked by the trading company (IQT), electronics and spare parts by the importer (USI, which runs FIFO so the demo shows layers), kitchenware, apparel and stationery by the Dubai entity (AEG); (2) opening stock is dated the first day of the current month (the only month the seeded books leave open for the general ledger) and valued from a USD draw converted at the official rate (IQD) or the peg (AED), rounded to the currency; (3) the seed's time budget becomes ten minutes (was one; it takes about six) because the 5,000 items and their opening stock go through the same services and posting engine a user's would, and the seed refreshes planner statistics mid-transaction through `app.refresh_statistics()` (V0021) so the costing walks and the harness are planned for the rows that exist; (4) lot numbers and expiry dates derive from the item ordinal, serial numbers are `{item code}-{sequence}`; (5) the costing engine's drain guard bounds the re-walks of one scope (50), not the number of scopes a document touches, so a document may carry any number of items. | proposed |
| A-105 | **M3 web pass** conventions: (1) every inventory screen works on the company the accounting screens remember (`quicker.company`), with a warehouse filter beside it; (2) document lines name items by code and the API resolves them; quantities and costs are typed as decimals and sent as strings, never as floating point; (3) status and kind vocabularies are translated by key with the raw value as the fallback, so a new status never blanks a screen; (4) master data a document needs (categories, brands, reason codes) is created from the screen that needs it rather than a separate admin area; (5) OpenAPI schema names are unique across modules and a collision fails the build; a corrected contract records what it removed and why in `contracts/contract-corrections.json` (ADR-0012). | proposed |
| A-106 | **Workflow core** (roadmap 4.0, ADR-0020): (1) approvers are named members or every active holder of a role (scoped to the document's company unless the step says otherwise); the requester's manager chain and dynamic owners wait for positions and the masters they point at; (2) the requester never approves their own document, at any step, even when their role is the approving role, and a step with nobody else left refuses the submission (`workflow.no_approvers`) rather than approving silently; (3) a decision is delivered to the owning module inside the approver's transaction and the module's refusal (a posting the books reject) fails the decision, so an approval never records against a document that did not move; the approval itself is the authority to post: the module posts without checking the approver's own post permission; (4) an override is single-use and expires after the definition's `override_valid_hours` (default 168, one week); the block stays open until an override exists and is cleared when one is consumed; a block with no active definition for its kind is recorded open and the module keeps refusing; (5) rules on blocks reason over the block's own values (typed from the values when raised), so the definition editor accepts field names the catalogue does not list for the `on_block` trigger and the run reports a type clash as `workflow.condition_invalid`; (6) `=` and `<>` are accepted as spellings of `==` and `!=`, and `x in ('a', 'b')` as well as `x in ['a', 'b']`, because that is how people write rules; (7) amounts convert through the spot rate of the day the rule runs, not the document's rate (the document's rate belongs to the posting, the threshold to policy); (8) an escalated step gains its target's members and keeps its original approvers, its SLA restarts, and a step without a target is reminded once and not again; (9) a cancelled request returns the document to draft, a rejection to rejected with the comment as the reason. | proposed |
| A-107 | **Supplier master** (roadmap 4.1, DOMAIN_MODEL §7): (1) a partner is one record per legal person, tenant-wide, wearing the supplier, customer and employee roles; the supplier account is per company and registering one gives the partner the supplier role, while the role cannot be removed while an account is active; (2) bank account numbers and IBANs are stored only encrypted under the platform key (ADR-0025) with a masked form for lists and prints; revealing the full identifiers is a separate permission and every reveal is an audit event naming the member, so a payment run can read them while a browsing user cannot; the IBAN is checked by mod-97 before it is accepted; (3) a supplier account's blank terms, delivery terms and posting group default from its group, and what documents read is the effective value; the account's own value always wins; (4) a hold is one of purchasing, payments or both, always with a reason and who placed it; a purchasing hold refuses new purchase documents but not paying what is already owed, a payment hold the reverse; (5) payment terms count from the invoice date, the end of its month or the delivery date; instalment percentages must sum to 100 and amounts are allocated with `RoundingPolicy` so the parts equal the whole in the currency's minor unit; "business days only" moves each due date to the company's next working day through the organisation calendar; the early-payment window counts from the base date; (6) Incoterms (EXW, FCA, FOB, CFR, CIF, DAP, DDP) and the usual payment terms (IMMEDIATE, NET15, NET30, NET60, EOM30) are seeded per tenant as system rows an admin may deactivate but not remove or recode; (7) withholding tax codes are kept by the partner master (`ptr_wht_codes`: rate, withheld at invoice or payment, optional threshold) until the tax engine of M5 owns them as `tax_codes` of kind `withholding`; the supplier account references them by id so the move is a table rename; (8) permission keys are `partners.supplier.read|manage`, `partners.supplier.reveal_bank_account` and `partners.terms.manage`; the purchaser template gets the terms permission, the accountant template the terms permission and supplier read, the payables clerk keeps supplier read. | proposed |
| A-108 | **Requisition to purchase order** (roadmap 4.2): (1) with no active workflow definition for a document type, a submission is approved at once; a matching rule makes it pending; (2) a change order always keeps the previous version as a numbered revision and goes through approval again, whatever the size of the change (re-approval policy per definition arrives with budget control); once anything has been received the order is changed through receipts, invoices and returns, not edited; (3) purchase-order tax is zero until the tax engine (M5) prices lines; totals carry the columns already; (4) budget commitments are recorded at approval per line on the role `Inventory` for stock items and `Expense` otherwise, in the month of the line's expected date (else the order's, else the order date), in the order's currency and the company's at the order's rate; they are released when the order is cancelled and will be consumed by invoices (M5) and checked against budgets (M6, `budget_exceeded` blocks); (5) the order is emailed as a bilingual HTML body with a text alternative; a PDF attachment comes with the print pipeline (5.9); (6) quotes are compared complete-first, then by the landed total (goods, freight, other charges) converted to the company's currency at today's spot rate, then by lead time, then by supplier code; the ranking is stored on the quote at the time of the comparison; (7) quotes are recorded by the buyer on the supplier's behalf until the supplier portal (M8); (8) requisition lines are marked ordered when the order draft is created, not when it is approved, so two buyers cannot order the same line twice; cancelling the draft gives the lines back; (9) blanket-agreement releases are counted at order approval and given back at cancellation; over-release is refused at submission with the agreed, released and requested quantities; (10) numbering: `REQ-`, `RFQ-`, `BPA-` and `PO-` yearly series per company, created on first use; (11) permission keys are `purchasing.requisition|rfq|agreement|order.read|manage` and `purchasing.order.send`; the purchaser template gets them all plus item and warehouse read. | proposed |
| A-109 | **Goods receipts** (roadmap 4.3, POSTING_RULES "Goods receipt"): (1) a receipt is against one order and only against stock items; services and expense items are settled on the supplier invoice without a receipt; (2) the expected cost is the order price after the line discount, converted at the spot rate of the receipt's posting date (not the order's rate), so GRNI is carried at the value the invoice will be matched against; the costing engine's rounding to the minor unit is the booked amount and the receipt keeps it; (3) over-receipt is allowed up to the supplier account's quantity tolerance per order line, counted across all receipts of the line; (4) a receipt is reversed as a whole through the stock engine's return applied to the original entries (exact cost), on or after its posting date, and only while nothing on it is invoiced or returned; partial returns are supplier returns (4.6); (5) the GRNI subledger item is the receipt, and the invariant "GRNI equals uninvoiced receipts" is checked per receipt in both directions by the harness after every purchasing scenario; (6) the order's received quantity is kept in the order line's unit, converted exactly through the base unit when the receipt uses another unit; a line is received when its received quantity reaches the ordered less cancelled quantity, and the order is received when every live line is; (7) receipts do not consume budget commitments; invoices will (A-108); (8) mobile receiving posts immediately rather than queueing, because a receipt changes stock and books value and must fail loudly at the dock; (9) permission keys are `purchasing.receipt.read|manage|post`; the purchaser and warehouse-operator templates get them. | proposed |
| A-110 | **Supplier invoices** (roadmap 4.4, POSTING_RULES "Supplier invoice"): (1) stock items are invoiced only against posted receipt lines (three-way); services and expense items against their order lines (two-way) or as free expense lines on an account role, `PurchaseExpense` by default, a role added to the chart of accounts (template account 6155 "Purchased services and supplies") so documents keep posting by role; (2) the tolerances are the supplier account's effective price and quantity tolerances; a breach beyond them blocks the invoice, and the block is cleared only by a workflow override of the same kind (`price_variance`, `qty_variance`, `duplicate_suspect`) consumed on the next submission; when nothing routes the block the invoice stays blocked and says so; (3) a duplicate suspect is another live invoice (submitted, blocked, approved or posted) of the same supplier with the same reference, compared case-insensitively; (4) the invoice price replaces the expected cost of every stock entry of the receipt line it invoices, whatever quantity it invoices; quantities already consumed are re-applied through the costing engine (COGS, or PPV for standard-cost items); a second invoice at a different price re-prices the entries again; (5) GRNI is settled at the invoiced value in the invoice currency at the invoice rate, and the receipt's invoiced value is allocated from the GRNI lines actually booked, so the GRNI invariant holds to the minor unit across currencies; (6) withholding at invoice reduces what is payable and credits `WhtPayable` (not a control account); the threshold applies only when it is in the invoice currency; (7) one payable open item is created per instalment of the supplier's payment terms, carrying the early-payment discount date and rate; the open item is settled by payments and credits (4.6–4.7); (8) the AP tables use the `ap_` prefix inside the procure-to-pay module (`Quicker.Purchasing`) until a separate payables module is warranted; (9) commitments are consumed by the invoiced value in the company's currency and marked consumed when fully used; (10) reversal mirrors the journal into the first open period, prices the receipt entries back to their expected cost and closes the open items, and is refused once anything was paid or credited; (11) permission keys are `purchasing.invoice.read|manage|post`, carried by the accountant and payables-clerk templates. | proposed |
| A-111 | **Landed costs** (roadmap 4.5, hard scenario 2): (1) charge types are tenant configuration with a default allocation basis; the usual five are seeded as system rows; (2) a landed-cost document allocates each charge across the receipt lines it names by value (the line's booked cost), quantity (base units), weight or volume (the item's per-unit figure × base units) using largest-remainder allocation in the company's currency, so the allocations sum to the charge to the minor unit; a line without the basis figure refuses that basis rather than taking zero; (3) the document posts through the costing engine's landed-cost adjustment on every stock entry of each receipt line, on or after the receipts' dates; the engine books stock on hand and pushes the consumed share to cost of sales (or PPV under standard cost); the on-hand and sold portions recorded on the allocation are the engine's own figures; (4) charges are estimates against `LandedCostClearing` (subledger item the document) until a supplier invoice line of kind `charge` settles them at the invoiced amount; the difference to the estimate is allocated onto the same lines in the same proportions, so the clearing account always carries exactly the unsettled estimates; a charge is settled once, only by the supplier expected to invoice it when one was named; (5) a document is reversed only while all its charges are estimates; reversing a charge invoice puts its estimate back; (6) the sold portion of a difference settled later follows the engine's re-application at that time; (7) permission keys are `purchasing.landed_cost.read|manage|post`, carried by the accountant and purchaser templates. | proposed |
| A-112 | **Supplier returns and debit notes** (roadmap 4.6, POSTING_RULES "Supplier return"): (1) a return is against one posted receipt and leaves stock at the exact cost the receipt's entries carry at that moment (invoice re-pricing and landed costs included), Dr GRNI with the return as the subledger item; (2) the supplier's credit is a debit note (an invoice of kind `debit_note`) whose return lines credit returned quantities, never more than went back; the GRNI credit is the returned cost and any difference to what the supplier credits is purchase price variance; no withholding on a debit note (the tax was withheld on the invoice it credits); (3) a return does not change the order's quantities and only reduces what the receipt can still be invoiced for by the returned quantity; (4) a posted debit note is a negative payable open item due at once (no payment-terms schedule) applied to invoices of the same supplier and currency; a rate difference between the two items is realised FX at application; (5) supplier advances (payment on account) belong with bank accounts and payments in 4.7; (6) a return is reversed only while nothing of it has been credited, a debit note only while it has not been applied, an invoice only while nothing has been applied to it. | default |
| A-113 | **Payables and banking** (roadmap 4.7, POSTING_RULES §4, ADR-0031): (1) the payables subledger is a module of its own that documents feed through a contract; purchasing opens and reverses its invoice items there; (2) every payment creates its own open item (kind `payment_on_account`, or `advance` on the supplier-advances control): the lines use it up and what is left stays on account, which keeps one settlement mechanism for credits, advances and payments and one reconciliation rule per control account; (3) a settled item leaves the books at its booked functional value; discount and withholding are valued at the payment rate; the bank moves at what the bank took; the functional difference is realised FX on the payment's own journal; a reversal mirrors it; (4) the early-payment discount is taken when an item is paid in full within its discount window, on the gross amount; a partial payment forfeits it; tax is withheld at payment on the gross amount of each line when the supplier's code says so; (5) a payment settles items of one currency and goes out of an account in that currency or in the company's currency (then the amount taken from the bank, charges included, is entered); (6) a proposal lists unheld invoice items due by the pay-through date or with an open discount, per currency, optionally per supplier; it is executed as draft payments per supplier; (7) bank and cash accounts post by role through a posting rule keyed on the account, registered when the account is created, so documents never name an account; (8) aging is computed at any date from open items and settlements dated up to it (settlement reversals are mirror rows, so a balance at a date is a plain sum), and a reversed item counts until its reversal date. | default |
| A-114 | **Supplier intelligence** (roadmap 4.8): (1) figures are computed from the documents on request, never snapshotted; (2) the scorecard's four dimensions are on time (by the order line's expected date, else the order's, plus a configurable tolerance), quantity kept (returns against receipts), price within the tolerance the invoice match applied, and invoices matched first time; a dimension without data drops out and the remaining weights are re-normalised; (3) grades are A ≥ 90, B ≥ 75, C ≥ 60, else D; (4) quotes appear in price history in their own currency; they are left out of the company-currency summary because a quote carries no rate; (5) the look-back window ends on the as-of date and covers whole months back from it. | default |
| A-115 | **Demo seed v4** (roadmap 4.9): the demo books hard-close every month before the last and stock goes live on the first of the current month, so "a year of purchases" is seeded as (1) eleven months of purchase orders from three suppliers per company, approved and then closed, which carry price history and do not post (they sit safely in the closed periods), and (2) the posted procure-to-pay cycle in the open current month only: an on-time supplier (received a day early, invoiced, freight landed two weeks after the receipt, paid in full), a late supplier (received five days after the promised date, ten units returned against a debit note, paid in half) and an open one (sixty of eighty received, invoiced, unpaid), plus one RFQ with three quotes left for comparison; in the trading company's warehouses, a transfer received, one in transit, a posted count with a reasoned variance and a frozen count waiting to be counted; the lot expiry sweep and one planner run per company, as the daily jobs would have done. Documents dated after today are clipped to today. The items are untracked (beverages, accessories, stationery) so no lot or serial numbers are invented. A full posted year of receipts and invoices waits for the demo books to be redesigned to close periods after the documents are posted, which is outside this additive slice. | default |
| A-116 | **Costing screens** (post-M4 routine pass): (1) inbound cost adjustments get no manual screen: they are how supplier invoices and landed-cost documents settle a receipt's cost, and a manual form would let stock cost change with no source document behind it (the API stays for integrations under `inventory.costing.manage`); (2) reservations get no screen yet: every reservation belongs to a source document and the only callers are the sales documents of M5, so a list would be empty by construction; (3) a company's costing method and cost pool are chosen on the company form when it is created and shown read-only afterwards, and an item's costing-method override is editable only while the item holds no stock in that company, because changing either re-values history (see open issue I2 for the server side); (4) the item's cost view shows today's figures on the item and the report date's on the valuation. | default |
| A-117 | **Set-up and security screens** (post-M4 routine pass): (1) a sensitive action refused for want of a recent sign-in is not an error to the person: the web app asks them to confirm who they are (password, or an authenticator code with MFA) and replays the refused request once, as the best SaaS consoles do ("sudo mode"), rather than sending them to sign out and in; the step-up renews only the access token and the session keeps its refresh token; (2) the sign-in policy refuses values that would lock the workspace (a step-up window under a minute, a lockout threshold under three attempts, password sign-in off without an active SSO connection) and an SSO connection that is the only way in while password sign-in is off cannot be disabled or deleted; (3) the policy's IP allow-list is not editable from a form, because a typo would lock out the administrator making it; it stays an API change; (4) an API key's secret is shown once, in the page that created it, and the list shows only its six-character tag after the tenant part (`qk_….abc123…`); (5) unit conversions are shown as stored (denominator from-units = numerator to-units), never as a computed decimal ratio; (6) the settings screen shows the switches the modules read today as plain choices and everything else in a typed key-value table, so a new module setting is visible before it gets its own control. | default |
| A-072 | **Permission keys are `module.entity.action`** (enforced by the catalogue), so ADR-0016's `numbering.reset` is `numbering.counter.reset`; period control keys live under `accounting.period.*` (`manage`, `reopen`, `post_in_soft_closed`) so the default SoD rule covers them; the `accountant` template grants `accounting.period.manage` and `post_in_soft_closed` but not `reopen` (and not `accounting.*`), otherwise the block rule "post journals versus reopen periods" would make the default role unassignable on its own. | proposed |
